server:
  port: 8080

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/product_database_dev?createDatabaseIfNotExist=true
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    servlet:
      path: /api/v1/product